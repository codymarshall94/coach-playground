import { ProfileSetupWizard } from "@/components/ProfileSetupWizard";

export const metadata = {
  title: "Set Up Your Profile — PRGRM",
};

export default function ProfileSetupPage() {
  return <ProfileSetupWizard />;
}
