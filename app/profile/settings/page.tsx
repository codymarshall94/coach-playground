import { ProfileSettings } from "@/components/ProfileSettings";

export const metadata = {
  title: "Profile Settings — PRGRM",
};

export default function ProfileSettingsPage() {
  return <ProfileSettings />;
}
